[tool.poetry]
name = "rename"
version = "0.1.0"
description = ""
authors = ["Hardy <hardyli719@outlook.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
colorama = "^0.4.6"
natsort = "^8.4.0"
pyotp = "^2.9.0"
requests = "^2.31.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
